{{ form_widget(form._token) }}
        {{ form_errors(form) }}

<div class="row">
    <div class="col-md-4 offset-md-4">{{ form_row(form.advertType) }}</div>
</div>
<div class="text-center my-5 border-bottom">
    <h2 class="display-6">{% trans %}Real estate Informations{% endtrans%}</h2>
</div>
<div class="row">
    <div>{{ form_row(form.name) }}</div>
</div>
<div class="row">
    <div class="col-6">{{ form_row(form.price) }}</div>
    <div class="col-6">{{ form_row(form.area) }}</div>
</div>
<div class="row">
    <div>{{ form_row(form.description) }}</div>
</div>
<div class="row">
    <div class="col-6">{{ form_row(form.type) }}</div>
    <div class="col-6">{{ form_row(form.totalRooms) }}</div>
</div>
<div class="row">
    <div class="col-6">{{ form_row(form.totalBathrooms) }}</div>
    <div class="col-6">{{ form_row(form.totalBedrooms) }}</div>
</div>

<div class="text-center my-5 border-bottom">
    <h2 class="display-6">{% trans %}Assets{% endtrans%}</h2>
</div>
<div class="row">
    <button type="button" class="col-md-6 offset-md-3 container text-center btn btn-primary rounded-0 my-4 text-uppercase btn-new" data-collection="#assets">{% trans %}Add Features{% endtrans%}
    </button>
    {{ form_errors(form.assets) }}
</div>

<div
        id="assets"
        class="row"
        data-prototype="{{ include('properties/_asset.html.twig', {form: form.assets.vars.prototype})|e("html_attr") }}"
        data-index="{{ form.assets|length > 0 ? form.assets|last.vars.name + 1 : 0 }}">
    {% for asset in form.assets %}

            {{ include('properties/_asset.html.twig', {form: asset}) }}

    {% endfor %}
</div>

<div class="text-center my-5 border-bottom">
    <h2 class="display-6">{% trans %}Agency Informations{% endtrans%}</h2>
</div>
<div class="row">
    <div class="col-md-8 offset-md-2">{{ form_row(form.linkWebsite) }}</div>
</div>
<div class="row">
    <div class="col-md-8 offset-md-2">{{ form_row(form.phoneContact) }}</div>
</div>
<div class="row">
    <div class="col-md-8 offset-md-2">{{ form_row(form.nameContact) }}</div>
</div>
<div class="text-center my-5 border-bottom">
    <h2 class="display-6">{% trans %}Captcha{% endtrans%}</h2>
</div>
<div class="row">
    <div class="col-8 col-md-4 offset-2 offset-md-4 text-center">{{ form_row(form.captcha) }}</div>
</div>
<div class="text-center my-5 border-bottom">
    <h2 class="display-6">{% trans %}Upload File{% endtrans%}</h2>
</div>
<div class="text-center my-5 border-bottom">


</div>
